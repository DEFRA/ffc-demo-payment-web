{{- include "ffc-helm-library.service" (list . "ffc-demo-payment-web.service") -}}
{{- define "ffc-demo-payment-web.service" -}}
metadata:
  annotations:
  {{- if .Values.ingress.alb }}
    {{- if .Values.pr }}
    external-dns.alpha.kubernetes.io/hostname: {{ .Values.ingress.paymentWebEndpoint }}-{{ .Values.pr }}.{{ .Values.ingress.server }}
    {{ else }}
    external-dns.alpha.kubernetes.io/hostname: {{ .Values.ingress.paymentWebEndpoint }}.{{ .Values.ingress.server}}
    {{- end }}
  {{- end }}
spec:
  ports:
  - port: {{ .Values.service.port}}
    name: http
    targetPort: http
  selector:
    {{- include "ffc-helm-library.selector-labels" . | nindent 4 }}
{{- end -}}
